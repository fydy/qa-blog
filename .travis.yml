language: node_js

nodejs:
  - stable

script:
  - npm install
  - npm run build:hash

after_script:
  - git init
  - git config user.name "fydy"
  - git config user.email "edu@yuns.me"
  - git add .
  - git commit -m "Update docs"
  - git push --quiet --force https://$qablog@github.com/fydy/qablog.git master
